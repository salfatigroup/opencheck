name: OpenCheck Self-Test

on:
  pull_request:

jobs:
  workflow-test:
    name: Reusable workflow integration
    uses: ./.github/workflows/opencheck.yml
    with:
      config: tests/fixtures/minimal-config.yaml
      local-build: true
      extra-args: "--validate"
